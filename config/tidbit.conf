# Copyright (c) 2020 The ZMK Contributors
# SPDX-License-Identifier: MIT

# Enable Encoders
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Enable underglow
CONFIG_ZMK_RGB_UNDERGLOW=y
# Use the STRIP config specific to the LEDs you're using
CONFIG_WS2812_STRIP=y

# Optional: tame initial brightness (0–100)
# CONFIG_ZMK_RGB_UNDERGLOW_BRIGHTNESS=40
# The symbol ZMK_RGB_UNDERGLOW_BRIGHTNESS is not present in this ZMK/Zephyr tree.
# If you need to set underglow brightness, replace the line above with the
# correct Kconfig symbol for your ZMK version (check ZMK Kconfig or run
# `menuconfig`/`west build` locally to discover available options).

# Uncomment to enable the display
# Note that an I2C OLED cannot be used at the same time as encoder 3.
#CONFIG_ZMK_DISPLAY=y
